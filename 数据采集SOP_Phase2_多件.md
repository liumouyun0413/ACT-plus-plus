数据采集 SOP — Phase 2（三类物件各多件混合）

一、方案定义
- 地面同时放 3~6 件物品，允许散件 + 堆叠并存。
- 物品：泡沫 / 塑料袋 / 物料板，每类 3~5 件。
- 合计 290 条：Stage-A' 60 + Stage-B' 80 + Stage-C' 120 + Stage-D' 30。
- 每个对象按自身位置独立分臂：左用左臂，右用右臂，中线 ±5 cm 的对象强制右臂。
- 一条 episode 内同一对象处理期间不换臂；切换对象时非主导臂先回 home ≥ 0.3 秒。
- 顺序规则固定：先清所有散件（由近及远）→ 再拆所有堆叠（由近及远，每堆从顶层拆）。
- 前提：Phase 1 的 policy_best.ckpt 已部署验收通过。

二、Stage 分工
Stage-A' 单对象巩固：每次 1 件（新物件批次），60 条，左右 30/30。
Stage-B' 多散件清桌：每次 3~6 件散件不接触，连续清空，80 条，左右 40/40。
Stage-C' 混合场景：散件 + 1~2 处堆叠并存，120 条，含 20 条"左按袋 + 右抓上层"协作剧本。
Stage-D' 失败恢复：多对象场景下自然/人为失败后成功恢复，30 条。

三、采集步骤（每一条 episode 都按这 8 步做）
步骤 1 — 确认起始状态
双臂都在 home
步骤 2 — 布置场景
按当前 Stage 摆放物品（A' 放 1 件新实例 / B' 放 3~6 件散件 / C' 放散件+堆叠 / D' 按失败类型布置）
所有对象避开中线 ±5 cm（中心带对象强制右臂处理）
步骤 3 — 口头报出执行顺序
先念散件"由近及远"次序，再念堆叠"由近及远"次序，辅助员记录
步骤 4 — 按下录制按钮
步骤 5 — 按顺序逐对象处理
每个对象：判定主导臂 → 主导臂从 home 出发 → 抓紧 → 抬升 → 放入收集框 → 非主导臂回 home ≥ 0.3 秒 → 再处理下一个
堆叠必须从顶层逐层拆；C' 协作剧本用"左手按压袋边 + 右手抓上层"
步骤 6 — 结束条件
桌面清空 或 总时长 120 秒（超时文件名加 _partial）
步骤 7 — 停止录制
步骤 8 — 双臂回 home
回到初始位 home，确认完全静止再开始下一条

四、290 条分配表
4.1 总表
Stage	内容	左主导	右主导	协作	小计
A'	单对象巩固（新实例）	30	30	0	60
B'	多散件清桌（3~6 件）	40	40	0	80
C'	混合：散件 + 堆叠	50	50	20	120
D'	多对象失败恢复	13	13	4	30
合计	—	133	133	24	290

4.2 Stage-B' 组合
3 件（三种各 1）30 条 / 4~5 件（两种各 2 + 一种 1）30 条 / 6 件（三种各 2）20 条。
散件间距 ≥ 8 cm，不准接触。

4.3 Stage-C' 组合
2~3 散 + 1 处 2 层堆 40 / 2~3 散 + 1 处 3 层堆 30 / 1~2 散 + 2 处 2 层堆 30 / 4 散 + 1 处 3 层堆 20。
协作剧本仅限"塑料袋底 + 泡沫或板上"，20 条全部执行"左按袋 + 右抓上层"。

4.4 Stage-D' 失败类型
散件中途滑脱 8 / 拆堆时带倒下层 8 / 对象推到中线带 6 / 抓错对象 5 / 协作剧本失败 3。
失败必须由同一只臂恢复并完成；换臂恢复 → 作废。

五、自然失败保留
同一只臂自己恢复并成功 → 保留，文件名加 _recov；
换臂 / 对象掉进中线禁采带没按规则恢复 / 一条失败 ≥ 3 次 / 最后没完成 → 整条作废。
Stage-D' 允许轻度人为制造失败（松手、轻推），但必须成功恢复；其他 Stage 严禁造假失败。

六、episode 边界
清空桌面 或 达到 120 秒。超时保留，文件名加 _partial 后缀；一条 episode 内不允许换人接管遥操。
