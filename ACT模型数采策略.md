# ACT-plus-plus 堆叠抓取 数据采集与训练策略

**场景**：对堆叠在一起的姿态随机的 **泡沫 / 塑料袋 / 物料板** 进行抓取
**模型**：ACT-plus-plus（从零训练，非 VLA 微调）
**目标**：用最少的高质量遥操数据，2~3 轮迭代内达到 ≥ 85% 抓取成功率

---

## 0. 核心理念

> **用数据的"分布覆盖"代替"数据量"**

- 135 条高多样性的数据 > 1500 条重复姿态的数据
- 不一次性采完再训，而是：采一批 → 训 → 评估 → 针对失败模式定向补采
- 绘制"数据多样性 vs 模型性能"曲线，哪个维度成功率低就补哪个维度

### 迭代补采闭环（核心工作流）

```
起步 135 条 → 训（8000 epoch 三段 warm-start）→ rollout 50 次
                      ↓
          记录失败模式（哪种物体 / 哪种姿态 / 哪种堆叠组合）
                      ↓
             针对失败模式补采 30~50 条
                      ↓
          warm-start 继续训（复用上一轮 ckpt）
                      ↓
     重复 2~3 轮，总量控制在 200~280 条，目标成功率 ≥ 85%
```

---

## 1. 硬件前置条件（数据质量的上限）

数据上限由硬件决定，硬件不达标再多数据也白搭。采集前务必确认：

| 项目 | 要求 | 原因 |
|---|---|---|
| 相机数量 | **3 路 RGB**：顶视 + 左腕 + 右腕 | 腕部相机是小样本泛化的决定性因素（消融显示去掉 wrist cam 成功率掉 30%+） |
| 同步精度 | ≤ ±10 ms | 异步会让 action chunk 学到错误的因果关系 |
| 分辨率 | 统一 480×640 | 避免 `constants.py` 里不同任务分辨率冲突 |
| 光照 | 顶灯 / 侧灯 / 混合共 3 档 | 反光物体对光照极敏感 |
| 反光处理 | 偏振滤光片 或 柔光布 | 塑料袋镜面反光会让 ResNet18 backbone 定位漂移 |
| 背景 | 可切换有/无参考物 | 减少过拟合到空桌面 |

---

## 2. 分阶段数据采集（Stage A/B/C/D，共 ~135 条起步）

与 `constants.py` 里 `stack_pickup_cotrain_p1/p2/p3` 配置严格对应。

| Stage | 内容 | 数量 | 角色 | 对应目录 |
|---|---|---|---|---|
| **A** | 桌面单件抓取（泡沫/袋/板，姿态随机） | **~50 条** | 基础技能：学"对准-闭合-提起"原子动作 | `stack_pickup/stage_A_single/act_dataset` |
| **B** | 两件堆叠，抓最上面那件 | **~30 条** | 过渡技能：处理单层遮挡 | `stack_pickup/stage_B_two_stack/act_dataset` |
| **C** | 三件堆叠，完整分拣流程（**目标任务**） | **~40 条** | 目标任务：最终部署场景 | `stack_pickup/stage_C_three_stack/act_dataset` |
| **D** | 失败-恢复（抓空/滑脱/碰翻重抓） | **~15 条**（占总量 ~11%） | 鲁棒性正则：让模型见过 OOD 状态 | `stack_pickup/stage_D_recovery/act_dataset` |
| **合计** | | **~135 条** | 最小可用数据集 MVP | |

> **为什么总量这么少？** ACT-plus-plus 是 ~30M 参数的小模型，不是 VLA。小模型 + 强模式化任务，100-200 条高质量演示就够启动。后续靠迭代补采（§5.2）逐步加到 200-280 条。

### 2.1 数据量 vs 性能 经验曲线

```
成功率
  95% ┤                              ___________
  90% ┤                    _________/
  85% ┤           ________/
  80% ┤     _____/              ← 边际收益开始递减的拐点
  70% ┤  __/
  60% ┤_/
      └────┬────┬────┬────┬────┬────┬────→ 数据量
           50   135  250  500  1000 2000 条
           ↑        ↑         ↑
          冷启动   推荐起步   边际收益≪投入
          能跑     甜点区    （除非多样性爆炸增长）
```

| 数据量区间 | 特征 | 每 +100 条收益 |
|---|---|---|
| < 80 条 | CVAE latent 学不充分，策略不稳定 | — |
| **100-200 条** | **MVP 甜点区**（135 条起步） | +8-12% |
| 200-500 条 | 边际收益良好，产线可用 | +3-5% |
| 500-1000 条 | 递减明显 | +1-2% |
| > 1000 条 | 基本饱和（除非多样性爆炸增长） | < +1% |

> **小模型的容量限制**：ACT-plus-plus ~30M 参数在 500 条后能表示的映射空间已接近极限。这是小模型和 VLA 大模型在"数据越多越好"问题上根本不同的原因。

### 2.2 三种"加大数据"的结局（警示）

| 情况 | 做法 | 结局 |
|---|---|---|
| ✅ **加大 + 高多样性** | 每条都随机化姿态/光照/背景 | 135→400 条后 +10-13% 成功率 |
| ❌ **加大 + 低多样性** | 相同姿态反复采凑数 | 不升反降（过拟合 + CVAE latent 坍缩） |
| ⚠️ **加大 + 失败数据失控** | Stage-D 占比 > 15% | 正常场景也学到"抖动、犹豫"风格 |

> **最常见的失败模式**：团队为了"凑数"加班采，结果越采越差。**加量不加多样性 = 扣分**。

### 2.3 数据量决策树（加采前先走这一步）

```
先采 135 条 MVP → 训完三段 warm-start → rollout 50 次评估
                            │
          ┌─────────────────┼─────────────────┐
          │                 │                 │
       < 70%            70% ~ 85%            ≥ 85%
          │                 │                 │
          ▼                 ▼                 ▼
   ⚠️ 停！先查:      定向补采 +80~150 条    ✅ 停！
   - 相机同步？        （§5.2 失败模式       直接进入
   - 光照反光？         → 补采映射表）       部署/压测
   - 遥操作质量？                            不要盲目加数据
   不要加数据，                              
   加了也没用
```

### 2.4 推荐的扩展方案（如团队人力充足）

> **覆盖测算前提**：§3.3 的随机化要求"位置 4 象限 × 朝向 4 档"共 **16 格**，稳定覆盖需每格 2-3 条。
> 下表的"位置象限覆盖 / 朝向×象限覆盖"列反映 **每种物体** 在 Stage-A 的格子平均条数。

| 方案 | 数据量 | 配比 [A,B,C,D] | 位置象限覆盖<br>（每物体 ÷4） | 朝向×象限覆盖<br>（每物体 ÷16） | 预期成功率 | 额外人力 | 适用 |
|---|---|---|---|---|---|---|---|
| **MVP（默认）** | 135 | 50/30/40/15 | ~4 条/象限 ✅ | ~1 条/格 ❌ | 65-75% | 3-4 人天 | 快速验证；朝向靠 B/C 泛化 |
| **方案 1** | 270 | 100/60/90/20 | ~8 条/象限 ✅ | ~2 条/格 ⚠️ | 82-87% | +6-8 人天 | 一次训到 85%，不做迭代 |
| **方案 1+**<br>（严格覆盖）| 320 | **150**/60/90/20 | ~12 条/象限 ✅ | ~3 条/格 ✅ | 85-90% | +9-11 人天 | 严格满足 §3.3 全维度随机化 |
| **方案 2（产线级）** | 500 | 180/120/160/40 | ~15 条/象限 ✅ | ~3.75 条/格 ✅ | 88-92% | +12-15 人天 | 产线部署，要求 ≥ 90% |
| 盲目加大到 1000+ | 1000+ | — | — | — | ≤ 90%（可能更低） | +30 人天 | ❌ 不推荐，边际收益差 |

> **关键取舍**：
> - **MVP 135 条 不满足 16 格全覆盖**（每格只有 ~1 条），录制时应**优先保证 4 象限都采到**，朝向随机变但不强求均匀 —— 靠 Stage-B/C 的数据让模型泛化朝向。
> - **方案 1 270 条** 每格勉强 2 条，边缘格子依然脆弱；如果任务对朝向鲁棒性要求高（例如塑料袋形状复杂），直接上 **方案 1+（320 条）** 把 Stage-A 提到 150 条。
> - **黄金法则**：**先 MVP → 按短板定向补采** 比 **一次采到 500** 省 60% 无效工作量。
> - 很多团队在 500 条采完后才发现相机不同步，数据全报废。

### 2.5 数据量扩展的硬约束

| 约束 | 数值 | 违反后果 |
|---|---|---|
| Stage-D 占总量比例 | **10% ~ 15%** | 超过 → 正常场景也学到抖动 |
| 单一堆叠组合占 Stage-C 比例 | **≤ 40%** | 超过 → 过拟合该组合 |
| 同一物体/姿态重复采 | **≤ 5 条** | 超过 → CVAE latent 坍缩 |
| 每轮补采量 | **30 ~ 50 条** | 超过 → 难定位是哪批数据起效 |

---

### 2.6 双臂分工规则（防模式坍缩）

> **问题背景**：ACT 的 CVAE 在推理时默认 `z = 0`（取先验均值），如果同一视觉状态下训练集同时出现"左臂抓"和"右臂抓"两种模态，模型会输出**两者的平均动作** —— 典型表现是**两只臂同时向物体伸、中途僵住、或来回摇摆**。这就是 **mode averaging / mode collapse**。
> 双臂 bimanual 任务（14 维 action = 左 7 + 右 7）最容易踩这个坑，采集阶段必须用**硬规则**让"状态唯一决定用哪只臂"。

#### 2.6.1 核心原则

**一个视觉状态 → 只允许一种臂的分工方案**。不要让遥操作员"凭手感"决定用哪只臂。

#### 2.6.2 硬空间分区规则（强制执行）

以机器人正前方桌面中线为界：

| 物体位置 | 主抓取臂 | 辅助臂 |
|---|---|---|
| 桌面**左半**（中线左 > 5 cm） | **左臂** | 不动 / 托扶 |
| 桌面**右半**（中线右 > 5 cm） | **右臂** | 不动 / 托扶 |
| **中心带**（中线 ±5 cm） | **不采集** | — |

> **为什么要留 ±5 cm 中心带**：边界附近本身是二义性状态（左右都合理），采了反而教坏模型。宁可少采，不可混采。
> **双手协作场景**（例如先掀开塑料袋再抓里面的物料板）例外处理 → 放到 Stage-C，且**固定剧本**：永远"左手掀 + 右手抓"或反之，不允许随机。

#### 2.6.3 各 Stage 的分工落地

| Stage | 总量 | 左臂主导 | 右臂主导 | 双臂协作 | 备注 |
|---|---|---|---|---|---|
| **A 单物随机姿态** | 150（方案 1+） | 75 | 75 | 0 | 严格按物体所在半区决定 |
| **B 多物无堆叠** | 30 | 15 | 15 | 0 | 按"先抓的那个"的位置决定主导臂 |
| **C 堆叠场景** | 40 | ~15 | ~15 | ~10 | 协作场景固定剧本（见上） |
| **D 失败/干扰恢复** | 15 | 7 | 7 | 1 | 左右失败各半，避免只练一侧恢复 |

#### 2.6.4 遥操作工位口令（贴在显示器旁）

```
【采集前必读】
1. 物体中心在桌面中线左侧 > 5cm  →  只用左臂，右臂保持 home
2. 物体中心在桌面中线右侧 > 5cm  →  只用右臂，左臂保持 home
3. 物体中心在中线 ±5cm 以内     →  跳过，不录！
4. 需要双手协作时，按固定剧本执行（左掀右抓 / 左抓右扶）
5. 一条 episode 中途不允许换臂（换臂 = 直接丢弃）
```

#### 2.6.5 推理期配套技巧

即使采集做对，推理时也建议：

- **开启 `--temporal_agg`**：对 chunk 内动作做指数加权平滑，能吸收少量 mode averaging 残留抖动。
- **z 从先验采样替代 z=0**（可选代码改动）：在 [detr/models/detr_vae.py](detr/models/detr_vae.py) 推理分支，把 `latent_sample = torch.zeros(...)` 改为 `latent_sample = torch.randn(...) * prior_std`，用采样代替均值。**需要配合 `--temporal_agg` 才稳定**。
- **两阶段推理（高阶方案，后期再做）**：先用一个轻量 "arm selector" head 根据视觉输出 `arm_id ∈ {L, R, both}`，再把 `arm_id` 作为条件塞回 policy。MVP 阶段不需要。

#### 2.6.6 自检清单

采集完 Stage-A 前 20 条后立刻检查：

- [ ] 同一物体位置的 episode，主导臂是否一致？
- [ ] 是否有物体在中线 ±5cm 范围内的 episode 被误录？
- [ ] 左右臂 episode 数量是否接近 50/50（±10%）？
- [ ] 有没有"一条 episode 内换臂"的废数据？

**任何一项不通过 → 停止采集，修正规则后重来。** 这个阶段每条废数据都会在后续训练中被放大。

---

## 3. 堆叠任务专项演示

### 3.1 抓取顺序决策

- **采集内容**：不同堆叠情况下"先抓谁、后抓谁"的完整序列
- **关键范式**：**先抓上层以暴露下层目标**
  - 例：泡沫压在塑料袋上 → 先抓泡沫 → 再抓袋
  - 例：板在最底、袋在中、泡沫在最上 → 依次从上到下

### 3.2 自适应抓取动作（三种物料的针对性演示）

| 物料 | 推荐抓取方式 | 避免动作 |
|---|---|---|
| **泡沫** | 夹爪侧向 / 对角抓取 | 垂直下压（易压扁变形） |
| **塑料袋** | 先铲 → 挑 → 卷 → 聚拢后再夹紧 | 直接夹（袋薄夹不住） |
| **物料板** | 夹爪末端轻触桌面 → 沿板边滑入 → 夹紧提起；底层被压时先移除上层 | 直接从正上方夹边缘 |

### 3.3 随机化清单（每回合强制改变，不要连采 10 条一样的姿态）

| 维度 | 要求 |
|---|---|
| 物体位置 | 工作区 4 象限均匀覆盖（桌面贴 10×10 cm 网格做指引） |
| 物体朝向 | 0° / 45° / 90° / 随机 各占 ~25% |
| 堆叠顺序 | 泡沫-袋-板、袋-板-泡沫 等全排列各采 2~3 条 |
| 光照 | 顶灯 / 侧灯 / 混合，至少 3 档 |
| 背景 | 桌面放 / 不放参考物 |
| 抓取策略 | 故意 50% 从侧面夹、50% 从上面捏 —— 让 CVAE 的 latent 学到多模态 |

### 3.4 失败-恢复数据（占总量约 10~11%，~15 条）

操作方法：
- 故意让夹爪闭合晚 0.3s → 抓空 → 重新对准抓
- 抓到一半松开 → 物体掉落 → 重新抓
- 堆叠被推歪 → 调整位姿再抓

> **这些数据不要丢弃！** ACT 的 chunk 预测会学到"发现异常就回到抓取起点"，显著提升真实部署时的鲁棒性。

---

## 4. 训练配方（对应 constants.py 的三段 warm-start 配置）

### 4.1 co-training 采样权重（三阶段课程）

| 阶段 | epoch 区间 | sample_weights [A,B,C,D] | 目的 |
|---|---|---|---|
| **Phase 1** | 0 ~ 2000 | **[3, 2, 2, 1]** | A 主导，打基础：先学单件抓取原子技能 |
| **Phase 2** | 2000 ~ 6000 | **[2, 2, 4, 1]** | C 主导（主配方）：收敛目标任务 |
| **Phase 3** | 6000 ~ 8000 | **[1, 2, 4, 2]** | D 加权：抛光鲁棒性，学失败-恢复 |

### 4.2 三段训练命令（ckpt 统一放扩展盘）

```bash
mkdir -p /home/liumouyun/extended_storage/liumouyun/checkpoints/stack_pickup

# Phase 1: 冷启动，2000 epoch
python3 imitate_episodes.py \
    --task_name stack_pickup_cotrain_p1 \
    --policy_class ACT --chunk_size 80 --kl_weight 10 \
    --hidden_dim 512 --dim_feedforward 3200 \
    --batch_size 8 --num_epochs 2000 --lr 1e-5 --seed 0 \
    --ckpt_dir /home/liumouyun/extended_storage/liumouyun/checkpoints/stack_pickup/p1

# Phase 2: 热启动 P1，4000 epoch
python3 imitate_episodes.py \
    --task_name stack_pickup_cotrain_p2 \
    --policy_class ACT --chunk_size 80 --kl_weight 10 \
    --hidden_dim 512 --dim_feedforward 3200 \
    --batch_size 8 --num_epochs 4000 --lr 1e-5 --seed 0 \
    --ckpt_dir         /home/liumouyun/extended_storage/liumouyun/checkpoints/stack_pickup/p2 \
    --resume_ckpt_path /home/liumouyun/extended_storage/liumouyun/checkpoints/stack_pickup/p1/policy_last.ckpt

# Phase 3: 热启动 P2，2000 epoch，学习率降半
python3 imitate_episodes.py \
    --task_name stack_pickup_cotrain_p3 \
    --policy_class ACT --chunk_size 80 --kl_weight 10 \
    --hidden_dim 512 --dim_feedforward 3200 \
    --batch_size 8 --num_epochs 2000 --lr 5e-6 --seed 0 \
    --ckpt_dir         /home/liumouyun/extended_storage/liumouyun/checkpoints/stack_pickup/p3 \
    --resume_ckpt_path /home/liumouyun/extended_storage/liumouyun/checkpoints/stack_pickup/p2/policy_last.ckpt
```

### 4.3 关键训练超参

| 参数 | 值 | 理由 |
|---|---|---|
| `--chunk_size` | 80 | 覆盖 ~2s 动作，对抓取重试事件跨度足够 |
| `--temporal_agg` | 评估时开 | 显著降低抖动，对反光物体尤其关键 |
| `--kl_weight` | 10 | 默认值，CVAE latent 正则强度 |
| Phase 1/2 lr | `1e-5` | 默认值 |
| Phase 3 lr | `5e-6` | 热启动后期降半，避免"震掉" P2 学到的策略 |

### 4.4 目录规范（所有大文件统一放扩展盘）

```
/home/liumouyun/extended_storage/liumouyun/
├── Datas/
│   └── stack_pickup/
│       ├── stage_A_single/act_dataset/
│       ├── stage_B_two_stack/act_dataset/
│       ├── stage_C_three_stack/act_dataset/   ← stats_dir 指向此处
│       └── stage_D_recovery/act_dataset/
└── checkpoints/
    └── stack_pickup/
        ├── p1/{policy_best.ckpt, policy_last.ckpt, dataset_stats.pkl}
        ├── p2/...
        └── p3/...
```

---

## 5. 评估与迭代补采

### 5.1 评估协议

每轮 Phase 3 训练结束后：
- **rollout 50 次**（环境姿态随机重置）
- **分维度统计成功率**：
  - 按物体：泡沫 / 塑料袋 / 物料板
  - 按 Stage：单件 / 两件堆叠 / 三件堆叠
  - 按堆叠组合：泡沫-袋-板 / 袋-板-泡沫 / ...
- **记录失败模式**：是识别不到、抓不稳、还是碰翻？

### 5.2 失败模式 → 定向补采策略

| 典型失败模式 | 定向补采内容 | 补采量 |
|---|---|---|
| 某种物体识别不到 | 该物体在各种光照/背景下的单件抓取（Stage-A） | +15~20 条 |
| 某个堆叠组合抓不对 | 该组合的 Stage-B/C 数据 | +15~25 条 |
| 抓失败后不会恢复 | Stage-D 失败-恢复数据 | +10~15 条 |
| 特定角度（如 45°）成功率低 | 该角度的 Stage-A/B 数据 | +10~15 条 |

### 5.3 收敛目标

| 迭代轮次 | 数据总量 | 预期成功率 |
|---|---|---|
| 起步（MVP） | ~135 条 | 65~75% |
| 第 1 轮补采 | ~175 条 | 75~82% |
| 第 2 轮补采 | ~220 条 | 82~87% |
| 第 3 轮补采（如需要） | ~270 条 | 87~92% |

---

## 6. 数据处理注意事项

采集完每一批数据后的必做检查：

1. **格式一致性**：四个 Stage 的 hdf5 必须共享相同的 `camera_names` / `action_dim` / `qpos_dim`
   - 用 `check_dataset_consistency_v2.py` 检查
   - 若不一致，用 `convert_to_act_hdf5_v2.py` 统一格式

2. **归一化统计**：`stats_dir` **只指向 Stage-C**（目标任务）
   - 若把 Stage-D 的失败抖动混入统计，action 方差会被拉宽，训练变慢
   - `constants.py` 里已按此配置

3. **episode_len 校准**：当前配置 `episode_len=600` 是占位值
   - 采完数据后用 `check_dataset_consistency_v2.py` 查真实 P95 帧数
   - P95 超过 600 时需调高

4. **采样权重生效验证**：训练开始后，第一个 epoch 日志里应该能看到各数据源的采样比例近似 `[3,2,2,1]/8 = [0.375, 0.25, 0.25, 0.125]`（P1 阶段）

---

## 附录：为什么不用 π0.5 / VLA 大模型？

| 考量 | ACT-plus-plus | π0.5 / VLA |
|---|---|---|
| 任务复杂度匹配 | ✅ 短时程、固定站点、强模式化任务正是甜点 | ❌ 为跨家庭、长时程、语义复杂任务设计，用不上 |
| 数据规模匹配 | ✅ 100~300 条可训练 | ❌ 后训练需 ~400h 数据 + 完整预训练权重（未公开） |
| 端侧部署（Thor aarch64） | ✅ ~30M 参数，>30 Hz 实时 | ❌ 数 B 参数 + flow matching 去噪，实时性难保证 |
| 工程投入 | ✅ 现有仓库已跑通 | ❌ 需重写 80% 代码 |
| 开放世界泛化 | ❌ 弱 | ✅ 强（但本任务不需要） |

**结论**：当前任务继续用 ACT-plus-plus，从 π0.5 借鉴的改进点（DINOv2 backbone / co-training / 子任务辅助 loss）作为增量优化。
